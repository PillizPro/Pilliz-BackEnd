SSH_SCRIPT_TO_EXECUTE = cd pilliz && ./pilliz-backend-staging.bash
SCP_SOURCE_FILE = env-staging/docker-compose.yaml
SCP_TARGET_DIR = /home/pillizpro/pilliz/env-staging

RESTE À FAIRE:
  - mettre en place un reverse-roxy nginx
  - donc ensuite faire le routage du backend staging et prod
  - donc supprimer la règle NAT du port 8081 correspondant au backend staging
  - faire la même chose que la branche staging pour la branche prod sur github avec tout les secrets etc dans l'environnement prod
  - donc finalement faire les différents routages du reverse-proxy nginx sur les ports web 80 et 443
  - et ajouter le certificat de sécurité https pour les routes de backend staging et prod
  - et ajouter les règles NAT correspondantes sur Azure dans le loadbalancer
