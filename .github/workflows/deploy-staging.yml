name: Deploy Staging

on:
  workflow_run:
    workflows:
      - "CodeQL"
    types:
      - "completed"
    branches:
      - "NL-docker-image-staging"
      # - "staging"

permissions:
  contents: read
  packages: write

jobs:
  deploy_staging:
    uses: PillizPro/Pilliz-BackEnd/.github/workflows/build-and-push.yml@staging
    secrets: inherit
    with:
      environment: staging